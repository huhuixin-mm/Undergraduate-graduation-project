# This file keeps the models directory in Git
# Actual model files are excluded by .gitignore due to their large size
# Download models using: git clone https://hf-mirror.com/OpenMeditron/Meditron3-Qwen2.5-7B